import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.Akomsstarter',
  appName: 'GigEcoApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
